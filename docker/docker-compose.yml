services:
  tlab:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    # Anahtarlar imaja GOMULMEZ, calisma aninda disaridan okunur.
    env_file:
      - ../.env
    volumes:
      - ../data:/app/data
      - ../config:/app/config:ro
    restart: unless-stopped
    command: ["doctor"]
